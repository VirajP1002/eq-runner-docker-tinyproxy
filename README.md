# eQ Runner Docker: Tinyproxy

This repo holds the Dockerfile used to build the tinypoxy image used for eq-runner-concourse

## Docker
The Dockerfile can be built using:
```bash
docker build -t eq-runner-docker-tinyproxy:latest .
```
